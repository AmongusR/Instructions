-- The first thing you need to do is install MonkeModManager and set up the following
-- Bepinex (always active), Then scroll down and select Computer Interface 1.6.1, Which should also select Bepinject and Extenject, Then select TMPLoader and Utilla
-- Then select Install/Update and at the bottom it should say "Status: Install complete!"
-- Go to Utilities and under Important Folders select Bepinex folder which should open File Explorer and select plugins
-- Next go to Installation in Instructions
